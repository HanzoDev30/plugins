plugins {
  id 'com.android.application'
}

android {
  namespace 'ir.ghostides.iconpack.mocha'
  compileSdk 36

  defaultConfig {
    applicationId 'ir.ghostides.iconpack.mocha'
    minSdk 26
    targetSdk 36
    versionCode 1
    versionName '1.0.0'
  }

  compileOptions {
    sourceCompatibility = JavaVersion.VERSION_17
    targetCompatibility = JavaVersion.VERSION_17
  }
}

dependencies {
  compileOnly fileTree(dir: 'libs', include: ['*.jar', '*.aar'])
}
