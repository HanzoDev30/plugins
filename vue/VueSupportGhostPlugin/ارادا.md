ادامه کار روی پلاگین Vue برای Ghost IDE - خروجی: /storage/emulated/0/AndroidIDEProjects/VueSupportGhostPlugin/vuesupport.gpl

وضعیت فعلی:
- پلاگین ساخته و بیلد میشه (bash build.sh VueSupportGhostPlugin، نتیجه vuesupport.gpl)
- گرامر TextMate (source.vue از Vetur) از assets لود میشه؛ log "Vue TextMate grammar registered (source.vue)" میاد
- هایلایت: FILE_EVENT برگشت داده شد؛ زبون TextMate با تم فعال ادیتور ست میشه و 4 بار reassert (300/900/2500/6000ms)
  - قبلاً مشکل این بود که LSP هنوز وصل نشده بود و هیچ کدوم از مسیرها زبون رو ست نمیکردن
- LSP: پکیج درست شد؛ npm install -g @vue/language-server (نه @volar). باینری: /usr/bin/vue-language-server --stdio
- پنل (VueEditorPanel) حذف شد چون NPE ریورس میداد؛ کاربر گفت "زبان سمت سروره"
- install script داخل assets/install-vue-lsp.sh هست و از Setup Action پلاگین اجرا میشه

برای تست هنوز:
1. پلاگین جدید رو نصب کن
2. Setup Action "install-vue-language-server" رو اجرا کن (در proot دبیان)
3. فایل sample.vue رو باز کن (همونجاست کنار پروژه)
4. چک کن: (الف) سینتکس رنگی بشه، (ب) دیگه خطای "Failed to connect to plugin LSP provider" نیاد، (ج) completion/diagnostic کار کنه

نکته: کاربر یه بار گفت انگار هایلایت رنگ نمیگرفت با اینکه گرامر ثبت شده بود - اگه باز مشکل داشت، موضوع یکی از ایناست:
- GPU/rendering نیست؛ چک کن لازما ThemeRegistry تم فعال رو داره (GhostColorScheme.syncRegistryTo)
- اگه LSP وصل شد و زبون رو به LspLanguage گرفت، alreadyHandled جلوی TextMate رو میگیره ولی رنگ باید بیاد
- شاید گرامر Vetur شامل هایلایت grammar های بیرونی (source.js/css) رو نداره پس داخل script/style کامل رنگ نمیگیره - اگه اینطوری بود، گرامر کاملتر جایگزین کن یا زبان embedded اضافه کن