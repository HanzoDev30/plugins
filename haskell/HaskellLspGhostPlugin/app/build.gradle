plugins {
  id 'com.android.application'
}

android {
  namespace 'ir.ghostide.haskelllsp'
  compileSdk 36

  defaultConfig {
    applicationId 'ir.ghostide.haskelllsp'
    minSdk 26
    targetSdk 36
    versionCode 1
    versionName '1.0.0'
  }

  compileOptions {
    sourceCompatibility = JavaVersion.VERSION_17
    targetCompatibility = JavaVersion.VERSION_17
  }
}

dependencies {
  compileOnly fileTree(dir: 'libs', include: ['*.jar', '*.aar'])
}